{
  "name": "aos-kernel-tutorials",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "playground:build": "node scripts/build-playgrounds.mjs",
    "test": "pnpm run playground:build && pnpm run build",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.5",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "sharp": "^0.35.3"
  }
}
